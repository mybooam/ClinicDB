<% drug = Drug.find(params[:drug_id]) %>

<div align=center>
	<% form_tag "update_drug" do %>
		<%= hidden_field :drug, :drug_id, :value => "#{drug.id}" %>
		<table class="layout" valign=top style="border: #aaa solid thin">
			<tr bgcolor=#F0F0FF>
				<td><label>Name:</label></td>
				<td><%= text_field :drug, :drug_name, :size => 20, :value => drug.drug_name %></td>
			</tr>
			<tr bgcolor=#FFFFFF>
				<td><label>Strength/Formulation:</label></td>
				<td><%= text_field :drug, :strength, :size => 20, :value => drug.strength %></td>
			</tr>
			<tr bgcolor=#F0F0FF>
				<td colspan=2> <%= submit_tag 'Save', :name => 'save' %> <%= submit_tag 'Cancel' %></td>
			</tr>
		</table>
	<% end %>
</div>